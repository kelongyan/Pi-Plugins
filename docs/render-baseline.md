# 渲染基线

> 本文件由 `npm run docs:baseline` 生成，**请勿手工编辑**。
> 渲染逻辑改动后重跑脚本，`git diff` 即为视觉变化。
> 基线使用纯文本主题，只反映结构与文本，不含配色。

共 36 个样例，宽度基准 72 列。

## minimal · 助手回复（裸排）

_minimal 下 assistant 正文原样输出（Pi 原生渲染），不加锚点、不画框_

```text
可以，我先确认它有没有副作用：

const sum = (a, b) => a + b;
```

## minimal · 用户消息（› 箭头锚点）

_minimal 下用户消息无框无背景：剥掉原生 userMessageBg 背景条，固定蓝色竖线引用块_

```text
│ 帮我把这个函数重构成纯函数
```

## minimal · 思考块（展开态）

```text
◆ Thinking · 先看调用方是否依赖内部状态…
│ 再看边界条件。
```

## minimal · 助手回复（assistantAnchor 开启）

```text
●
测试全部通过。
```

## minimal · 思考块（展开态）

```text
◆ Thinking · 先看调用方是否依赖内部状态…
│ 再看边界条件。
```

## minimal · 工具调用（进行中）

```text
● read · 读取 src/index.ts …
```

## minimal · 工具调用（成功）

```text
✓ edit · 已修改 3 处
```

## minimal · 工具调用（失败）

```text
× bash · Exit code 1
```

## minimal · Bash 执行

```text
● bash · $ pnpm test
│ ✓ 78 passed
```

## minimal · 自定义消息

```text
· 来自扩展的消息
```

## minimal · 上下文压缩摘要

```text
· 已把前 20 轮对话压缩为要点
```

## minimal · edit diff 语法高亮（diffHighlight 开启）

_增删行的 +/- 符号与行号保留语义色，代码内容经 Pi 官方 highlightCode 重新着色；上下文行透传保持弱化灰_

```text
✓ edit · src/utils/diff.ts
│  41 export function parseDiff(line: string) {
│ -42   const parts = line.split(" ");
│ +42   const parts = line.split(/\s+/);
│  43   return { prefix: parts[0], rest: parts.slice(1) };
│  44 }
```

## minimal · 思考块（折叠态）

_hideThinkingBlock 为 true 时内容只有一行，自然只剩锚点行_

```text
◆ Thinking · 正在分析调用链…
```

## minimal · 窄终端降级（宽度 30）

_锚点行与 gutter 行都不超过给定宽度_

```text
✓ edit · 这一行比较长，用来观
```

## minimal · 极窄终端回退（宽度 6）

_低于最小宽度时放弃锚点排版，只做截断_

```text
● abcd
```

## boxed · 用户消息

_以下为 boxed 兼容风格；用户消息在任何风格下都用经典外框（与输入框线框呼应）_

```text
╭─ USER ───────────────────────────────────────────────────────────────╮
│ 帮我把这个函数重构成纯函数                                           │
╰──────────────────────────────────────────────────────────────────────╯
```

## boxed · 助手回复（含代码块）

```text
╭─ ASSISTANT ──────────────────────────────────────────────────────────╮
│ 可以，我先确认它有没有副作用：                                       │
│                                                                      │
│ ```ts                                                                │
│ const sum = (a: number, b: number) => a + b;                         │
│ ```                                                                  │
╰──────────────────────────────────────────────────────────────────────╯
```

## boxed · 思考块（展开态）

```text
╭─ THINK ──────────────────────────────────────────────────────────────╮
│ 先看调用方是否依赖内部状态…                                          │
│ 再看边界条件。                                                       │
╰──────────────────────────────────────────────────────────────────────╯
```

## boxed · 工具调用（进行中）

```text
╭─ TOOL read ──────────────────────────────────────────────────────────╮
│ 读取 src/index.ts …                                                  │
╰──────────────────────────────────────────────────────────────────────╯
```

## boxed · 工具调用（成功）

```text
╭─ TOOL edit ✓ ────────────────────────────────────────────────────────╮
│ 已修改 3 处                                                          │
╰──────────────────────────────────────────────────────────────────────╯
```

## boxed · 工具调用（失败）

```text
╭─ TOOL bash ✗ ────────────────────────────────────────────────────────╮
│ Exit code 1                                                          │
╰──────────────────────────────────────────────────────────────────────╯
```

## boxed · Bash 执行

```text
╭─ BASH ───────────────────────────────────────────────────────────────╮
│ $ pnpm test                                                          │
│ ✓ 78 passed                                                          │
╰──────────────────────────────────────────────────────────────────────╯
```

## boxed · 自定义消息

```text
╭─ CUSTOM ─────────────────────────────────────────────────────────────╮
│ 来自扩展的消息                                                       │
╰──────────────────────────────────────────────────────────────────────╯
```

## boxed · 上下文压缩摘要

```text
╭─ COMPACT ────────────────────────────────────────────────────────────╮
│ 已把前 20 轮对话压缩为要点                                           │
╰──────────────────────────────────────────────────────────────────────╯
```

## boxed · 思考块（折叠态：紧凑三行）

_hideThinkingBlock 为 true 时使用_

```text
╭─ THINK ──────────────────────────────────────────────────────────────╮
│ 正在分析调用链…                                                      │
╰──────────────────────────────────────────────────────────────────────╯
```

## boxed · 窄终端降级（宽度 30）

_外框必须保持闭合，内容按可用宽度折行_

```text
╭─ ASSISTANT ────────────────╮
│ 这一行比较长，用来观察折行 │
│ 与外框闭合是否正确         │
╰────────────────────────────╯
```

## boxed · 极窄终端回退（宽度 6）

_低于最小宽度时放弃画框，只做截断_

```text
abcdef[0m
```

## 输入框线框（默认：不显示模型名）【保留项】

```text
╭──────────────────────────────────────────────────────────────────────╮
│ > 帮我把这个函数重构成纯函数                                         │
╰──────────────────────────────────────────────────────────────────────╯
```

## 输入框线框（显示模型名 + thinking + 上下文）【保留项】

```text
╭ Atria-Dawn-Preview · high ──────────────────── ━━━━━━────── 42% 256k ╮
│ > 继续                                                               │
╰──────────────────────────────────────────────────────────────────────╯
```

## 底部状态栏（全段）【保留项】

_位于输入框下方；宽度不足时从右往左整段丢弃，永不截断段内文字_

```text
 model Atria-Dawn-Preview(high) │ dir project │ git main │ ctx 18.8% (48k/256k) │ tok 4.6k │ tps 18.4 tps
```

## 底部状态栏（窄宽度降级）【保留项】

_宽度 70：先丢速度与 token，再丢上下文，保留模型 / 目录 / 分支_

```text
 model Atria-Dawn-Preview(high) │ dir project │ git main
```

## 启动画面 · 自定义 logo header【startup】

_ctx.ui.setHeader 接管后的开屏第一屏（前置：Pi quietStartup 已开启）；右列三行与 logo 第 2 行起对齐_

```text
████████████  
    ██    ██  Pi v0.84.4
    ██    ██  / commands · ! bash · ctrl+o more
    ██    ██  ● ready
  ████    ██  

◆ Resources · tools 27 · commands 34 · skills 3 · mcp 2
```

## 启动画面 · 窄终端降级（宽度 30）【startup】

```text
Pi v0.84.4
```

## bash 单行 · 运行中（spinner 动态帧）【bash-inline】

_单行折叠：spinner 按时间片轮换（实机 1Hz 借力 Pi 的 Elapsed 重绘），输出预览运行中不显示_

```text
⠏ bash · running pnpm test · 3.2s
```

## bash 单行 · 成功完成【bash-inline】

_命令收进摘要行（原生 toolTitle 蓝色整行被丢弃），耗时并入锚点；输出保留折叠预览并进 gutter_

```text
✓ bash · pnpm test · 2.3s

│ [2m  passed  78[0m
│ [2m  failed  0[0m
│ [2m  ... (9 earlier lines, ctrl+o to expand)[0m
```

## bash 单行 · 失败完成【bash-inline】

_× 锚点（error 色），错误输出原样保留_

```text
× bash · pnpm test · 2.3s

│ [2m  ✗ should merge cells (2ms)[0m
│ [2m  exit code 1[0m
```

